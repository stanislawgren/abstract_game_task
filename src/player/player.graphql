type Query {
  players: [Player!]!
  player(id: ID!): Player!
  player_games(id: ID!): PlayerGames!
}

type PlayerGames {
  ID_player: Int!
  first_name: String!
  last_name: String!
  number: Int!
  ID_team: Int!
  games: [GamePlayerDetails!]!
}

type Player {
  ID_player: Int!
  first_name: String!
  last_name: String!
  number: Int!
  ID_team: Int!
  team: Team!
}
